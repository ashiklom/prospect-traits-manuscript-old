targets:
    results_wide_bysp:
        command: dat.results_bysp(results_wide)

    results_wide_residual:
        command: dat.results_species_residuals(results_wide)

    results_long:
        command: dat.filter_results(results_all_long, usemodel)

    results_wide:
        command: dat.results_wide(results_raw_long, spectra_info)

    results_all_long:
        command: dat.results_all_long(results_raw_long, spectra_info)

    results_raw_long:
        command: dat.results_raw_long(specdb_path, resultsdb_path)

