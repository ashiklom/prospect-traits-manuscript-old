packages:
  - tidyverse
  - methods
  - riri
  - lubridate
  - assertr
  - forcats

sources: R

include:
  - dat.results.yml
  - dat.traits.yml
  - dat.site.yml
  - paths.yml
  - figures.yml

targets:
  all:
    depends:
        - figures

  usemodel:
    command: c(I('PROSPECT 5B'))

  spectra_info:
    command: dat.spectra_info(specdb_path, results_raw_long)
